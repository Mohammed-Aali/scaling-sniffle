# اسال المستخدم عن اسمه
# strip method on string (removes white space from str)
name = input("What's your name? ").strip().title()

# split user's name into first name and last name
# assigns the splitted values to first and last variables
# first, last = name.split(' ')

# escape
# print('hello, \"friends\"')

# capitalize method on string (capitalize first letter)
# name = name.capitalize()

# Capitalize name like a title
# name = name.title()

#format string
print(f'hello, {name}')

# سلم عليه
# end default is '\n' which creates a new line
# sep default is ' ' which is a space

# paramaters are possible input for functions
# arguemnts are the paramaters occupied
# print('hello,',name, sep='???')

